# Full documented DREDGE/MEDiCINe sweep on the early100s snippet

User requested both implementations from `luke_ap_motion_methods_20260908.md` across the full sigma/screen sweep. This job compares40 combinations:2 methods ×3/4/5/6/8σ ×no/center-only/relaxed/full screens,using exactly the completed sweep's peak and location arrays on930–1030s. No voltage extraction,redetection or screening refit is needed.

DREDGE follows documentedv8:0.25s bins,0.5s Gaussian histogram sigma,20 nonrigid windows and the separately versioned strict±250µm helper. MEDiCINe followsv6:0.25s bins,1s triangular kernel,4 depth bins,motion_bound500,10000steps,batch4096,network256/256,Adam0.0005,seed0,CUDA,and no amplitude quantile cutoff. Its bound and temporal kernel have different meanings from DREDGE's. The rescue virtualenv runs DREDGE; the existing anaconda spikeinterface environment runs MEDiCINe. CUDA availability was verified outside the tool sandbox on the RTX A5000.

The17 lighthouse candidates and strict/lower-score/ambiguous evidence remain unchanged. Fixed seed-depth interpolation,identical seed-spike timestamps within common field support,and common event-time support are used. No field extrapolation or fit of sign,gain,lag,identity or later reference offset. Descriptive summaries separate all observations from fixed|waveform displacement|≥120µm observations and require≥5 events per candidate/class/scope. No automatic winner or production adoption.

Cached unscreened5σ DREDGEv8/MEDiCINev6 fits were reused after exact input peak/location equality checks. All20 input peak/location hashes passed preflight; both baseline fields are finite400-time arrays and contain all17 seed depths. The remaining38 combinations are fresh fits. Baseline comparison against lighthouses is already rendered as `testing/outputs/luke_ap_methods_sweep_v1/00_cached5sigma_lighthouse_comparison.pdf`.

Implementation: `testing/luke_ap_methods_sweep_v1.py`, `testing/luke_ap_methods_sweep_fit_v1.py`, `testing/luke_ap_methods_sweep_report_v1.py`. Scientific output: `testing/outputs/luke_ap_methods_sweep_v1/`. Independent systemd service: `luke-ap-methods-sweep-v1`; launch command,stdout/stderr and final service receipt: `testing/outputs/luke_ap_methods_sweep_v1_job/`. Individual fits have their own persistent commands,stdout/stderr,exit receipts,settings/runtime audit and recursive checksums. Sequential execution bounds resource use. Completed arms can be reused after checksum verification; an interrupted fit has no optimizer checkpoint and its unsealed evidence must be investigated before restart.

Separate post-launch inspection verified active/running MainPID2101629 after the launcher disconnected. The existing systemd method was previously dummy-tested for launcher-independent lifetime. This records launch status,not completion. Check actual manager state and final receipts before reporting later progress.

Final reports are generated by the same managed process: separate all17-candidate sweep PDFs for DREDGE and MEDiCINe,plus predeclared5σ/8σ head-to-head examples,per-event predictions,per-candidate differences and amplitude tables. Baseline and full-report layouts should be inspected before scientific handoff.
