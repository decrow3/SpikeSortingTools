# Direct lighthouse evidence before consensus

The consensus coverage count is not a test of whether the underlying motion is recoverable. The user's correction is now encoded in AGENTS.md: direct existence check, replication, sanity controls, precision estimation only after evidence, optimization last.

Cached accepted observations were plotted directly for all25quality-eligible candidates, with no new binning, interpolation, DREDGE, or consensus. Twenty-two have accepted events. Individual-event energy centroids use transported spatial support and each unit's seed-template centroid as reference; this noisy statistic remains uncalibrated for physical movement.

Reciprocal nearest spike pairs outside930–940s training, at most0.25s apart, yield27within-region nearby-cell comparisons (nominal depth separation≤400µm). Each event appears at most once per pair. Pair scatter shows every matched point; allpair statistics are saved. The overview chooses onepair perregion solely by matching count, not agreement.

There is a concrete positive local lead:388/398 near2000/2040µm show common negative excursions in the direct scatter. Of42paired observations,19have both centroids below−20µm; median absolute paired difference is6.9µm. Shared drops occur around958–960,975–979,999–1000and1015–1018s. Some paired observations disagree markedly. This example was selected after viewing the data and is exploratory. Unweighted seed-template cosine on the same1920–2120µm support is0.670; this alone does not certify independent biological identities.

Other regions contain little common motion, sparse observations or cell-specific jumps. Agreement nearzero is not evidence that motion has been recovered, and missing matches can still reflect recognition bias. Neither a local agreeing pair nor the old two-point consensus count settles DREDGE correctness. Next inspect concrete candidate waveforms/identity alternatives at a shared excursion and seek another independent pair before building a new estimator.

Plots andCSV: `testing/outputs/luke_lighthouse_direct_check_v1/`. Reproduce with `python -m testing.luke_lighthouse_direct_check_v1` then `python -m testing.luke_lighthouse_direct_pair_example_v1` using the production virtualenv. No new voltage extraction or heavy jobs were launched.
