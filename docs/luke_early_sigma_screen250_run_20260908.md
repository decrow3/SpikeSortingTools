# Early100s sigma/screening sweep with±250µm pairwise bounds

Authorized by the user after reviewing the17 waveform-only candidates against the earlier±80µm DREDGE field. Interval930–1030s. Detector thresholds3,4,5,6,8σ crossed with no screen,center-only,relaxed combined and full screen:20 factorial arms at±250µm. A21st field uses5σ/no screen/±80µm on the exact same common-interior detections. Before fresh extraction,a cheap bound-only replay uses the complete cached5σ peaks at±250µm against the existing±80µm field. This separates bound widening from sigma and screening changes.

The17 candidates and their strict/lower-score/ambiguous observations are frozen. All fields use fixed seed-centroid depth and seed-spike temporal reference; compare at actual event times with no fitted gain,sign,lag or subsequent candidate reselection. Report all observations and the predeclared|waveform-relative displacement|≥120µm subset separately,by evidence class. Require≥5 events for a per-candidate/class/scope difference summary. A median across candidate summaries is descriptive,not a verified independent-cell consensus or accuracy score. No automatic winner will be adopted. The working interval has already been inspected.

Detector noise stays the original frozen per-channel MAD; screening uses the separately frozen residual noise. Preprocessing,compensation,localization and non-bound DREDGE settings remain unchanged. Fresh5σ detection must reproduce cached sample/channel keys and amplitudes on common interiors; all thresholds exclude50samples at20s chunk edges. Localize the threshold union once,reusing matching cached5σ locations. Save union features and all arm masks for reuse. No sort or motion application to voltage is launched.

Implementation: `testing/luke_early_sigma_screen250_v1.py`, `testing/luke_early_sigma_screen250_report_v1.py`, new generalized helper `testing/luke_dredge_bounded_range.py`. The historical80µm helper is unchanged. Prelaunch checks passed: synthetic±200µm peaks recovered with250µm and excluded with80µm; inclusive250µm boundary excludes300µm alternatives; a3s cached integration reproduces old80µm pairwise shifts exactly and its field numerically,then completes at250µm with finite bounded results. Source compilation passed.

Independent job: `luke-early-sigma-screen250-v1`. Evidence: `testing/outputs/luke_early_sigma_screen250_v1_job/`. Scientific outputs: `testing/outputs/luke_early_sigma_screen250_v1/`. Reuses the independently dummy-tested systemd launch method. Report generation runs inside the same managed job after fields complete. Completed20s chunks and fields are hash validated for explicit relaunch; interrupted stages have no internal checkpoint and unsealed evidence causes a refusal until investigated. Source/settings changes require a new version. Preserve failures and holds.

The launcher returned successfully. A separate systemctl inspection verified active/running MainPID2087292 after launcher disconnection; the observation is saved in the job directory. This is launch verification, not completion evidence. Check the service and final receipts for subsequent status.

## First result: cached bound-only control

The identical-input5σ control completed. Median across20 depths of temporal peak-to-peak displacement increased from60.8677µm at±80µm to185.5594µm at±250µm (about3.05×). The250µm field spans−163.7619 to102.0349µm; this is a spatial/time global range, not one cell's excursion. Pairwise hypotheses reach250µm. This establishes substantial bound sensitivity, not accuracy. The full sigma/screen sweep continues separately. Initial comparison figure: `testing/outputs/luke_early_sigma_screen250_v1/00_cached_bound80_vs250.pdf`.
