# Frozen lighthouse extension through1230s

Authorized extension of the17 frozen waveform-only candidates from930–1030s through1230s. Reuse original events; extract only1030–1230s. The extraction function is text-identical to the original global matcher except for its time loop. Cached waveforms, all247 competitors,49sample16channel relative geometry,40µm patch translations and±3sample temporal alignments remain unchanged. Absolute depth, the MEDiCINe field and temporal continuity do not enter matching.

Strict, lower-score and ambiguous display rules match the frozen expansion. All detections, unmatched events, scores, gains, lags and rivals remain in5s chunk artifacts. Geometry checks verify translation invariance and centroid sign. Incomplete probe-edge patches remain excluded. This is a temporal generalization test of provisional identities, not certified cell ground truth.

Independent job: `luke-lighthouse-extension-300s-v1.service`. Launch command, logs and exit receipt: `testing/outputs/luke_lighthouse_extension_300s_v1_job`. Results: `testing/outputs/luke_lighthouse_extension_300s_v1`. Completed5s chunks are checksum-verified on explicit relaunch; unsealed evidence refuses reuse. No within-chunk checkpoint; interrupted chunk must restart only after investigation.

After matching completes, the report loads the three existing five-minute MEDiCINe fields. It preserves the same seed-time offset convention, plots absolute waveform depths over the5σ/relaxed screened peak background, and compares fields with individual waveform observations. New1030–1230s support and discrepancies are tabulated separately by evidence class and excursion size. No smoothed waveform path, consensus, or later offset fitting is introduced. Fields and screened peaks are comparison evidence only; waveform extraction uses original bandpassed globally median-referenced voltage.

Launch-time validation: source equivalence check and Python compilation passed; independent service checked active after launcher exit. Completion requires final summary and successful receipt, not this launch note.
